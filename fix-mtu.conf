# Multi-WAN PPPoE MTU Configuration
# Format: "PPP_INTERFACE:WAN_INTERFACE:VLAN_ID:MTU"
# Leave VLAN_ID blank if not using a VLAN (e.g., "ppp1:eth7::1500")

CONNECTIONS=(
    "ppp0:eth6:35:1500"
    "ppp1:eth7::1500"
)
